% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/center_of_mass.R
\name{calculate_center_of_mass}
\alias{calculate_center_of_mass}
\title{Set a NIfTI image's origin to its center of mass}
\usage{
calculate_center_of_mass(path, write = TRUE)
}
\arguments{
\item{path}{Character. Path to a NIfTI file (\code{.nii}/\code{.nii.gz}).}

\item{write}{Logical. If \code{TRUE} (default), overwrite \code{path} on disk with
the updated header. If \code{FALSE}, the modified image object is returned
without writing anything.}
}
\value{
Invisibly, the modified \code{niftiImage} object (see
\code{\link[RNifti]{readNifti}}).
}
\description{
Computes an approximate center of mass for a NIfTI image — the centroid
of voxels whose intensity exceeds the image mean — and rewrites the
image's sform rows so that point becomes the new origin.
}
