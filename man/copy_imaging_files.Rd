% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copy.R
\name{copy_imaging_files}
\alias{copy_imaging_files}
\title{Copy NIfTI imaging files into an organized directory structure}
\usage{
copy_imaging_files(
  datadir,
  output_datadir,
  f0 = 1,
  f1 = 0,
  organize_files_by = c("participant", "scan"),
  overwrite = FALSE
)
}
\arguments{
\item{datadir}{Character. Path to the source directory containing imaging
files.}

\item{output_datadir}{Character. Path to the destination directory.}

\item{f0}{Numeric (default = 1). File index to start with (default = 1).
Index refers to the filenames sorted in alphabetical order.}

\item{f1}{Integer. Index of the last file to copy. If \code{0} (the default),
all files from \code{f0} to the end of the listing are copied.}

\item{organize_files_by}{Character. How to organize copied files: \code{"participant"}
(default) creates one subfolder per participant, derived from
\code{pattern}; \code{"scan"} copies all files into a single subfolder named
after \code{scan}.}

\item{overwrite}{Logical. Overwrite files that already exist at the
destination? Default \code{FALSE}.}
}
\value{
Invisibly, a character vector of the destination paths that were
copied (files that were skipped because they already existed and
\code{overwrite = FALSE} are not included).
}
\description{
Copies NIfTI files (\code{.nii} or \code{.nii.gz}) from a source directory into a
destination directory, organized either by participant (using a regex
\code{pattern} to extract a participant ID from each filename) or by scan type
(\code{mri}/\code{pet}, placed into a single subfolder).
}
\examples{
\dontrun{
# Copy all files, organized into one folder per participant
copy_imaging_files(
  from    = "raw_scans",
  to      = "sorted_scans",
  by      = "participant",
  pattern = "^(sub-[0-9]+)"
)

# Copy only the first 10 files, organized by scan type
copy_imaging_files(
  from = "raw_scans",
  to   = "sorted_scans",
  f1   = 10,
  by   = "scan",
  scan = "pet"
)
}

}
